export const initialState= {
    agenda:""
}


export function storeReducer(state, action) {

    switch (action.type) {
        case "changeAgenda":
            return state;
    
        default:
           return state;
    }
    
}

